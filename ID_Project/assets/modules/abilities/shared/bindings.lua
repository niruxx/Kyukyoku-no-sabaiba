-- modules/abilities/shared/bindings.lua
-- Shared binding definitions for abilities.
-- Used by both client (key polling) and server (validation).

return {
    fireball = { key = "KeyQ", mode = "game" },
}
